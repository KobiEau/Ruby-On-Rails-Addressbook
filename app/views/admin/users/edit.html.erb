<% content_for :page_title, "Edit User" %>

<div class="max-w-2xl mx-auto space-y-4 px-4 sm:px-0">

<%= render "shared/back_link", text:"← Back to Users",path: admin_users_path%>
  <div>
    
     <h1 class="text-2xl font-black text-[var(--color-text-heading)]">
      Edit User
    </h1>
    <p class="text-sm text-[var(--color-text-body)]">Fill in the details below</p>
    
      <%= render "form", user: @user%>
  </div>
</div>