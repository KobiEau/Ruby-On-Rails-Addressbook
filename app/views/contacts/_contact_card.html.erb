<div class="bg-[var(--color-surface-card)] rounded-lg border border-[var(--color-border-subtle)] shadow p-4 max-w-[280px] w-full mx-auto">

  <div class="relative">
    <%# Checkbox - top-right corner %>
    <label class="absolute top-3 right-3 z-10 cursor-pointer">
      <input type="checkbox"
            class="contact-checkbox w-4 h-4 accent-[var(--color-accent)]
            cursor-pointer"
            value="<%= contact.id%>"
            data-action="change->export#toggle">
    </label>
  </div>

  <%# Avatar + Name %>
  <div class="flex items-center mb-3">
    <div class="<%= contact_initials_color(contact) %> text-[var(--color-text-white)] rounded-full w-10 h-10 
        flex items-center justify-center font-bold text-lg shrink-0">
      <%= contact_initials(contact) %>
    </div>

    <div class="ml-4">
      <h3 class="text-base font-bold text-[var(--color-text-heading)]">
        <%= contact.firstname %> <%= contact.lastname %>
      </h3>
      <p class="text-sm text-[var(--color-text-body)]">
        <%#= contact.phone_number %>
      </p>
    </div>
  </div>

  <%# Single view action %>
  <div class="mt-4">
    <%= link_to "View Contact", contact_path(contact),
          class: "block w-full text-center text-sm font-semibold px-3 py-2
                  /*bg-[var(--color-btn-card)] hover:bg-[var(--color-btn-card-hover)]*/
                  bg-[var(--color-view-toggle)] hover:bg-[var(--color-view-toggle-hover)]
                  text-[var(--color-text-white)] rounded-[var(--radius-md)]
                  transition-colors duration-200" %>
  </div>

</div>